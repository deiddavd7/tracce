// Array iniziale di nomi
let nomi = ["Daniel", "Vincenzo", "Antonio", "Alberto"];

// Utilizzo del metodo map per aggiungere il testo a ciascun nome
let nomiModificati = nomi.map(nome => `${nome} - ho imparato il metodo map`);

// Stampa l'array modificato
console.log(nomiModificati);
